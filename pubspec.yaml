name: ptnet_plugin
description: "A new Flutter plugin project."
version: 1.0.0+1
homepage:

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  permission_handler: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.example.ptnet_plugin
        pluginClass: PtnetPlugin
      ios:
        package: com.example.ptnetPlugin
        pluginClass: PtnetPlugin